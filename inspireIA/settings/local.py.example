"""
Local settings for inspireIA project.

This file is for local development settings that should not be committed to version control.
Copy this file to local.py and customize as needed.
"""
from typing import Dict, Any, List

# Override any settings from base, development, or production here
# Examples:

# DATABASES: Dict[str, Dict[str, Any]] = {
#     'default': {
#         'ENGINE': 'django.db.backends.postgresql',
#         'NAME': 'inspireIA_local',
#         'USER': 'postgres',
#         'PASSWORD': 'your_password',
#         'HOST': 'localhost',
#         'PORT': '5432',
#     }
# }

# INSTALLED_APPS: List[str] = [
#     # Add local development apps here
#     'debug_toolbar',
# ] + INSTALLED_APPS

# MIDDLEWARE: List[str] = [
#     # Add local development middleware here
#     'debug_toolbar.middleware.DebugToolbarMiddleware',
# ] + MIDDLEWARE

# INTERNAL_IPS: List[str] = [
#     # For Django Debug Toolbar
#     '127.0.0.1',
# ]

# DEBUG = True

# ALLOWED_HOSTS: List[str] = ['localhost', '127.0.0.1', 'your-local-domain.test']

# Custom API keys for local development
# OPENAI_API_KEY = 'your-openai-api-key'
# HUGGINGFACE_API_KEY = 'your-huggingface-api-key'
